import type { Character } from "../types/swapi";
/**
 * TODO: Implement the `filterCharacters` function to filter the given `characters` array based on the `selectedGender`.
 */
export const filterCharacters = (
  characters: Character[],
  selectedGender: string,
): Character[] => {
  return characters;
};
